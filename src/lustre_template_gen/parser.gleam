//// Template parser for `.lustre` files.
////
//// Converts template source text into tokens and builds an AST representing
//// the template structure including HTML elements, expressions, and control flow.
